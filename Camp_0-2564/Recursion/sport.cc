#include <bits/stdc++.h>
using namespace std;

int n;
int a[220];

void rec(int w, int l, int state){
	if(w == n || l == n){
		for(int i=0; i<=; ++i){
		
		}
	}
}

int main(){
	int w, l;
	cin >> n >> w >> l;
	rec(w, l, 0);
	return 0;
}
